## identibooru

read contents of `run.sh`


### requirements

```
apt -y upgrade
apt -y install python3 python3-pip python3-virtualenv

```

### install
*works in bash only*
```
./run.sh install
./run.sh debug

```